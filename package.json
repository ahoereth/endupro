{
  "name": "endupro",
  "version": "0.1.0",
  "private": true,
  "description": "Endurance progress tracker powered by Intervals.icu",
  "scripts": {
    "start": "FITBOARD_DEV_LIVE_RELOAD=1 node --watch --watch-path=server.js --watch-path=public server.js",
    "start:server": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
